# Session with Alessio

- Palettes switch mode
- If a component should not switch based on palette mode, it needs a hand-crafted layer around (cards on accent in light mode which are white instead of black like in real dark mode)
